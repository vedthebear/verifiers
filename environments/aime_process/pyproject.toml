[project]
name = "aime-process"
version = "0.1.0"
description = "AIME process-reward eval environment: answer correctness + per-step LLM-judged reasoning validity + efficiency metrics."
tags = ["aime", "math", "single-turn", "eval", "process-reward", "judge"]
requires-python = ">=3.10"
dependencies = [
    "verifiers>=0.1.15.dev0,<0.2.0",
    "openai>=1.50",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
    "aime_process.py",
    "aime_parsers.py",
    "aime_rubrics.py",
    "aime_prompts.py",
    "pyproject.toml",
    "README.md",
]

[tool.verifiers.eval]
num_examples = 5
rollouts_per_example = 2
